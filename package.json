{
  "name": "email-os",
  "version": "0.1.0",
  "description": "Self-hosted, lightweight email marketing system (contacts, newsletters, sequences) replacing GetResponse, sent via Resend.",
  "private": true,
  "type": "commonjs",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json && node -e \"require('fs').cpSync('src/views', 'dist/views', { recursive: true }); require('fs').cpSync('src/public', 'dist/public', { recursive: true })\"",
    "start": "node dist/index.js",
    "migrate": "node dist/db/migrate.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "resend": "^6.16.0",
    "ejs": "^3.1.10",
    "express": "^4.19.2",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "@types/ejs": "^3.1.5",
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.0",
    "@types/pg": "^8.11.6",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0"
  }
}
